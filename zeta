#!/bin/bash

declare -r __GO_CORE="${0%/*}/scripts/go-script-bash/go-core.bash"

if [[ ! -f "$__GO_CORE" ]]; then
  git submodule update --init
fi
. "$__GO_CORE" "scripts"
. "$_GO_USE_MODULES" 'log'


@go "$@"



